/**
 * mul - multiplies 2 numbers
 * @a: first number
 * @b: second number
 * Description: takes in two numbers, multiplies them and return the answer
 * Return: product
 */
int mul(int a, int b)
{
	int ans;

	ans = a * b;

	return (ans);
}
