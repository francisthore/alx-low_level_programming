#include <strig.h>

/**
 * _strstr - locates substring
 * @haystack: main string
 * @needle: substring
 * Return: pointer to substring
 */

char *_strstr(char *haystack, char *needle)
{
	char *prt = strstr(haystack, needle);

	return (ptr);
}
