#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: second string
 * Return: -ve value or 0 or +ve value
 */

int _strcmp(char *s1, char *s2)
{
	int i;

	i = strcmp(s1, s2);

	return (i);
}
