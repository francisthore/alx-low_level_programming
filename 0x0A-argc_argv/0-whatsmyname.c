#include <stdio.h>

/**
 * main - runs the code
 * @argc:  arguments counter
 * @argv: array of arguments from the cmd
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
